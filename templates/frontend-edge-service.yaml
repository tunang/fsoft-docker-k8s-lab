apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: {{ .Values.namespace.edge }}

  labels:
    {{- include "notes.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend-edge

spec:
  type: {{ .Values.frontend.edgeService.type }}
  externalName: {{ .Values.frontend.edgeService.externalName }}